// Demo entries for docs previews — merged by `pnpm build:registry`.
// Do not import this file directly from apps/www.

export const demoEntries = {
  "drawer-slide": ({ theme = "dark" }) => <DrawerDemo theme={theme} />} as const;
